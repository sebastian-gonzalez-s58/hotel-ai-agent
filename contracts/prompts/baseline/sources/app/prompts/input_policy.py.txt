ORDER_QUANTITY_POLICY = """Quantities must be positive whole numbers supported by the guest's original words.
Number words in any language and articles that unambiguously mean one (such as 'a burger')
are valid evidence. Never default an absent, uncertain, fractional or conflicting quantity to 1.
For a partial edit, retain an existing quantity unless the guest explicitly changes it.
If a new item's quantity is missing, ask for it; do not invent it or discard the existing order."""
